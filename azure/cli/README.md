# cli

Cheat sheet for Azure CLI commands to speed up day-to-day tasks. Add short, copy-paste ready snippets here.

- `kv-get-all.sh` – list all secret names and latest values from a Key Vault. Usage: `./kv-get-all.sh <vault-name>` (prints to stdout; handle securely).
